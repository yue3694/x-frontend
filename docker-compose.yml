services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.go
    ports:
      - "8080:8080"
  web:
    build: .
    environment:
      GO_API_URL: http://api:8080
    ports:
      - "3000:3000"
    depends_on:
      - api
